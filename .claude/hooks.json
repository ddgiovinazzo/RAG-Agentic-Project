{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "mkdir -p ./notes && printf '%s\n' '# Exploration mode' '' 'This is exploration mode. Source is read-only. Propose changes as markdown notes in ./notes/.' > ./notes/proposed-change.md && echo 'Exploration mode active. Source is read-only. Propose changes as markdown notes in ./notes/.'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "mkdir -p ./notes && echo \"$(date -Iseconds) READ $TOOL_FILE\" >> ./notes/exploration-log.txt"
          }
        ]
      }
    ]
  }
}
